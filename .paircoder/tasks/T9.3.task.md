---
id: T9.3
title: docs/setup.md — install + run in 3 commands
plan: plan-2026-03-docs-and-readme
type: chore
priority: P0
complexity: 15
status: pending
sprint: 9
tags: [docs]
depends_on: []
trello_card_id: "52"
---

# Objective

Create docs/setup.md with dead-simple setup instructions. A judge should clone, install, and have the app running in under 2 minutes.

# Files to Update

| File | Change |
|------|--------|
| docs/setup.md | NEW — setup guide |

# Implementation Plan

1. Read backend and frontend requirements/configs to understand actual deps
2. Write docs/setup.md with:
   - **Prerequisites**: Python 3.11+, Node 18+, npm
   - **Quick Start** (3 commands):
     1. Clone + install backend deps
     2. Install frontend deps
     3. Start both servers
   - **Environment Variables**: what to set and where (.env for backend, .env.local for frontend), with examples from .env.example files
   - **Database**: SQLite auto-creates, seed data loads on first run
   - **Optional Services**: Credit API (sibling project), BrightData API key, Claude API key, EmailJS keys
   - **Running Tests**: backend pytest, frontend vitest
   - **Troubleshooting**: common issues (port conflicts, missing env vars)

# Acceptance Criteria

- [ ] docs/setup.md exists with prerequisites
- [ ] Quick start section with 3 commands to get running
- [ ] Environment variable reference for both backend and frontend
- [ ] Optional services section (credit API, BrightData, Claude, EmailJS)
- [ ] Test commands documented
- [ ] Instructions actually work when followed

# Verification

```bash
# File exists
wc -l docs/setup.md
```
