---
id: T7.3
title: BrightData Routes — POST /crawl, GET /status, POST /precrawl
plan: plan-2026-03-brightdata-live-jobs
type: feature
priority: P0
complexity: 35
status: done
sprint: null
tags: []
depends_on:
- T7.1
- T7.4
trello_card_id: '41'
completed_at: '2026-03-05T02:55:34.746645'
---

# Objective

Flesh out brightdata route stub with endpoints to trigger crawls, check status, and run pre-crawl. All endpoints return 503 when API key not configured.

# Files to Update

| File | Change |
|------|--------|
| `backend/app/routes/brightdata.py` | MODIFY: add POST /crawl, GET /status/{id}, POST /precrawl |
| `backend/app/integrations/brightdata/types.py` | MODIFY: add TriggerCrawlRequest, response models |
| `backend/tests/test_brightdata_routes.py` | NEW: full test coverage |

# Implementation Plan

1. Write tests: trigger success, no API key (503), validation error, status running, status ready with auto-cache, precrawl
2. Add request/response Pydantic models to types.py
3. POST /api/brightdata/crawl — accepts URLs, returns snapshot_id immediately
4. GET /api/brightdata/status/{snapshot_id} — polls BrightData, auto-caches when done
5. POST /api/brightdata/precrawl — delegates to precrawl function (T7.5)
6. Config guard: check brightdata_api_key, return 503 if empty

# Acceptance Criteria

- [x] POST /crawl returns snapshot_id
- [x] GET /status returns progress or completion with job count
- [x] Returns 503 when API key not configured
- [x] Auto-caches results to job_listings on completion
- [x] POST /precrawl triggers pre-crawl pipeline
- [x] 100% test coverage with mocked client

# Verification

```bash
cd backend && python -m pytest tests/test_brightdata_routes.py -xvs
bpsai-pair arch check backend/app/routes/brightdata.py
```