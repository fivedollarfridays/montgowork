---
id: T7.5
title: Pre-Crawl Montgomery Jobs — seed Indeed/LinkedIn data
plan: plan-2026-03-brightdata-live-jobs
type: feature
priority: P1
complexity: 25
status: done
sprint: null
tags: []
depends_on:
- T7.1
- T7.2
- T7.4
trello_card_id: '43'
completed_at: '2026-03-05T03:06:51.893382'
---

# Objective

Create management function to pre-populate job_listings by crawling Indeed/LinkedIn for Montgomery, AL job searches. Skip if recent data exists.

# Files to Update

| File | Change |
|------|--------|
| `backend/app/integrations/brightdata/precrawl.py` | NEW: build_search_urls, precrawl_montgomery_jobs |
| `backend/tests/test_brightdata_precrawl.py` | NEW: full test coverage |

# Implementation Plan

1. Write tests: URL generation, trigger+poll+cache flow, staleness skip, no API key, empty results
2. Define MONTGOMERY_SEARCH_URLS — Indeed (general, warehouse, healthcare, customer service) + LinkedIn
3. Implement `build_search_urls() -> list[str]`
4. Implement `precrawl_montgomery_jobs(db_session) -> dict` — trigger + poll + cache
5. Staleness check: skip if brightdata source rows < 24h old
6. Return `{"snapshot_id": str, "jobs_cached": int, "skipped": bool}`

# Acceptance Criteria

- [x] Generates correct Indeed/LinkedIn Montgomery URLs
- [x] Triggers crawl, polls until complete, caches results
- [x] Skips if recent data (< 24h) exists
- [x] Handles missing API key gracefully
- [x] 100% test coverage

# Verification

```bash
cd backend && python -m pytest tests/test_brightdata_precrawl.py -xvs
bpsai-pair arch check backend/app/integrations/brightdata/precrawl.py
```