---
id: T7.4
title: Job Listing Cache — parse, deduplicate, insert crawl results
plan: plan-2026-03-brightdata-live-jobs
type: feature
priority: P1
complexity: 30
status: done
sprint: null
tags: []
depends_on:
- T7.1
trello_card_id: '42'
completed_at: '2026-03-05T03:01:03.345127'
---

# Objective

Map BrightData crawl results to job_listings schema, deduplicate by URL, and bulk insert. Add query functions for insertion and source-based retrieval.

# Files to Update

| File | Change |
|------|--------|
| `backend/app/integrations/brightdata/cache.py` | NEW: parse_brightdata_jobs, store_crawl_results |
| `backend/app/integrations/brightdata/types.py` | MODIFY: add BrightDataJobRecord model |
| `backend/app/core/queries.py` | MODIFY: add insert_job_listings, get_job_listings_by_source |
| `backend/tests/test_brightdata_cache.py` | NEW: full test coverage |

# Implementation Plan

1. Write tests: parse valid/missing fields, insert, dedup by URL, source tagging, expiry
2. Add BrightDataJobRecord Pydantic model to types.py
3. Add `insert_job_listings(session, listings) -> int` to queries.py
4. Add `get_job_listings_by_source(session, source) -> list[dict]` to queries.py
5. Implement `parse_brightdata_jobs(raw_jobs)` — lenient, title required
6. Implement `store_crawl_results(session, snapshot_id, raw_jobs)` — parse + dedupe + insert
7. Source tag: `brightdata:{snapshot_id}`, expires_at = scraped_at + 30 days

# Acceptance Criteria

- [x] parse_brightdata_jobs handles missing fields gracefully
- [x] store_crawl_results inserts with source tag and 30-day expiry
- [x] Deduplication by URL prevents duplicates
- [x] insert_job_listings bulk inserts to DB
- [x] get_job_listings_by_source filters correctly
- [x] 100% test coverage

# Verification

```bash
cd backend && python -m pytest tests/test_brightdata_cache.py -xvs
bpsai-pair arch check backend/app/integrations/brightdata/cache.py
```