---
id: T5.8
title: Wire plan/page.tsx — MondayMorning + BarrierCards + JobMatch
plan: plan-2026-03-demo-killer-frontend
type: feature
priority: P0
complexity: 35
status: done
sprint: 5
tags:
- wiring
- demo-critical
depends_on:
- T5.3
- T5.4
- T5.5
- T5.6
trello_card_id: '32'
completed_at: '2026-03-05T00:34:36.672803'
---

# Objective

Replace the plan placeholder with the full plan view. Reads session_id from URL params, fetches plan data, and renders MondayMorning hero, BarrierCards, JobMatch cards, ComparisonView, and AI narrative generation button.

# Files to Update

| File | Change |
|------|--------|
| frontend/src/app/plan/page.tsx | Full plan view with all plan components |

# Implementation Plan

1. "use client" page, reads `session` from searchParams
2. useQuery to GET /api/plan/{session_id} — fetches plan data
3. Layout sections (top to bottom):
   - MondayMorning hero (immediate next steps)
   - BarrierCard grid (one per barrier)
   - Job Matches section with JobMatch cards
   - ComparisonView ("Now vs 3 Months")
   - "Generate AI Summary" button → useMutation POST /api/plan/{session_id}/generate
4. Loading skeleton while fetching
5. Error state if session not found (404)
6. Suspense boundary for async data

# Acceptance Criteria

- [x] Reads session_id from URL search params
- [x] Fetches plan data via useQuery
- [x] Renders MondayMorning hero section
- [x] Renders BarrierCard for each barrier in the plan
- [x] Renders JobMatch cards for matched jobs
- [x] Renders ComparisonView
- [x] "Generate AI Summary" button triggers narrative generation
- [x] Loading skeleton shown while fetching
- [x] 404 error handled gracefully
- [x] `npm run build` passes

# Verification

```bash
cd frontend && npm run build
# Manual: complete assessment → verify plan page loads with all sections
```