---
id: T5.7
title: Wire assess/page.tsx — wizard flow with real API calls
plan: plan-2026-03-demo-killer-frontend
type: feature
priority: P0
complexity: 35
status: done
sprint: 5
tags:
- wiring
depends_on:
- T5.1
- T5.2
trello_card_id: '31'
completed_at: '2026-03-05T00:30:47.109640'
---

# Objective

Replace the assessment placeholder with a working wizard that collects user data and calls POST /api/assessment/. Uses WizardShell with BarrierForm. On success, redirects to plan page with session_id.

# Files to Update

| File | Change |
|------|--------|
| frontend/src/app/assess/page.tsx | Full wizard with WizardShell, BarrierForm, work history step, submission |

# Implementation Plan

1. "use client" page with WizardShell containing 3 steps:
   - Step 1: Zip code + employment status (basic info)
   - Step 2: BarrierForm (barrier selection + has_vehicle)
   - Step 3: Work history textarea + review summary
2. useMutation from TanStack Query for POST /api/assessment/
3. On success: router.push(`/plan?session=${data.session_id}`)
4. Loading state during submission (button disabled, spinner)
5. Error handling: toast or inline error message
6. Form validation: require zip code, at least 1 barrier

# Acceptance Criteria

- [x] 3-step wizard flow with WizardShell
- [x] Step 1 collects zip + employment status
- [x] Step 2 uses BarrierForm with severity preview
- [x] Step 3 collects work history + shows review
- [x] Submits to POST /api/assessment/ with correct payload shape
- [x] Redirects to /plan?session={id} on success
- [x] Shows loading state during submission
- [x] Validates required fields before advancing
- [x] `npm run build` passes

# Verification

```bash
cd frontend && npm run build
# Manual: start backend + frontend, complete wizard flow, verify redirect to plan page
```