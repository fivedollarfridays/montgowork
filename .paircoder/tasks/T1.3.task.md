---
id: T1.3
title: Populate seed data JSON files
plan: plan-2026-03-module-skeletons
type: feature
priority: P0
complexity: 15
status: done
sprint: null
tags:
- data
- seed
- json
depends_on: []
trello_card_id: '3'
completed_at: '2026-03-04T19:25:30.936375'
---

# Objective

Overwrite the existing empty `[]` seed data files in `data/` with real Montgomery community resource data. These files are the foundation for the matching engine.

# Files to Update

| File | Change |
|------|--------|
| `data/career_centers.json` | 1 record: Montgomery Career Center |
| `data/training_programs.json` | 4 records: MRWTC, AIDT, Trenholm, MPACT |
| `data/childcare_providers.json` | 3 records: DHR Subsidy, Head Start, Family Guidance Center |
| `data/community_resources.json` | 5 records: 2-1-1, JOBS Program, MATS, Salvation Army, Food Bank |
| `data/transit_routes.json` | 14 records: Routes 1-14 with schedules |
| `data/montgomery_businesses.json` | Leave as `[]` (populated later from Open Data Portal) |

# Implementation Plan

1. Overwrite `career_centers.json` with Montgomery Career Center record
2. Overwrite `training_programs.json` with MRWTC, AIDT, Trenholm, MPACT
3. Overwrite `childcare_providers.json` with DHR, Head Start, Family Guidance Center
4. Overwrite `community_resources.json` with 5 social service records
5. Overwrite `transit_routes.json` with 14 M Transit routes (all sunday: false)
6. Leave `montgomery_businesses.json` as `[]`
7. Validate all JSON files parse correctly

# Acceptance Criteria

- [x] `career_centers.json` has 1 record with services array and hours
- [x] `training_programs.json` has 4 records including MPACT (reentry subcategory)
- [x] `childcare_providers.json` has 3 records
- [x] `community_resources.json` has 5 records including MATS transit info
- [x] `transit_routes.json` has 14 routes, all with `sunday: false`
- [x] `montgomery_businesses.json` remains `[]`
- [x] All JSON files are valid (parseable)
- [x] All records have `name` and `category` fields (required by resources table)

# Verification

```bash
python -c "
import json
from pathlib import Path
for f in Path('data').glob('*.json'):
    data = json.loads(f.read_text())
    print(f'{f.name}: {len(data)} records')
"
```