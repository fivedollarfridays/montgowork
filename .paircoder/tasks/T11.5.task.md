---
id: T11.5
title: Frontend polish — loading states, fetch timeout
plan: plan-2026-03-launch-prep-polish
type: chore
priority: P1
complexity: 20
status: pending
sprint: 11
tags: [frontend, ux]
depends_on: []
trello_card_id: "63"
---

# Objective

Add loading spinner for live jobs section and implement fetch timeout via AbortController.

# Files to Update

| File | Change |
|------|--------|
| frontend/src/app/plan/page.tsx | Show Loader2 spinner while liveJobs query is pending |
| frontend/src/lib/api.ts | Add AbortController with 30s timeout to apiFetch |

# Implementation Plan

1. **plan/page.tsx**: Destructure `isLoading` from liveJobs useQuery. Render spinner before jobs grid
2. **api.ts**: Create AbortController, setTimeout(30s), pass signal to fetch, clearTimeout on completion

# Acceptance Criteria

- [ ] Live jobs shows loading spinner while fetching
- [ ] API calls abort after 30 seconds
- [ ] Build passes, existing tests pass

# Verification

```bash
cd frontend && npm run build && npm test
```
