---
id: T3.4
title: Test app lifespan, root endpoint, credit success path, and type imports
plan: plan-2026-03-test-coverage
type: chore
priority: P1
complexity: 15
status: done
sprint: 3
tags:
- testing
depends_on: []
trello_card_id: '14'
completed_at: '2026-03-04T20:31:03.657697'
---

# Objective

Cover remaining gaps: `main.py` (83% → 100%), `routes/credit.py` (93% → 100%), `core/logging.py` (90% → 100%), and 4 type-only modules at 0%.

# Files to Update

| File | Change |
|------|--------|
| `backend/tests/test_main.py` | New — test root endpoint + lifespan |
| `backend/tests/test_credit_proxy.py` | Add success path test (200 response) |
| `backend/tests/test_types.py` | New — import-only coverage for ai/types, brightdata/types, data/types, documents/types |

# Implementation Plan

1. Test `root()` endpoint via test client: GET / returns status dict
2. Test lifespan: mock `get_engine` + `init_db`, verify startup/shutdown sequence
3. Test credit proxy success: mock httpx returning 200 with valid JSON, assert pass-through
4. Test `get_logger()`: call and assert returns a logger instance
5. Type imports: instantiate each Pydantic model (AnalysisResult, PlanNarrative, CrawlRequest, etc.) to trigger coverage

# Acceptance Criteria

- [x] `main.py` at 100% coverage
- [x] `routes/credit.py` at 100% coverage
- [x] `core/logging.py` at 100% coverage
- [x] `ai/types.py`, `brightdata/types.py`, `data/types.py`, `documents/types.py` at 100%
- [x] All tests pass

# Verification

```bash
pytest tests/test_main.py tests/test_credit_proxy.py tests/test_types.py -v
pytest tests/ --cov=app --cov-report=term-missing -q
```