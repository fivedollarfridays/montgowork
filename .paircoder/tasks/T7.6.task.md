---
id: T7.6
title: Wire Live Jobs into Plan View — merge scraped + seed listings
plan: plan-2026-03-brightdata-live-jobs
type: feature
priority: P1
complexity: 35
status: done
sprint: null
tags: []
depends_on:
- T7.4
- T7.5
trello_card_id: '44'
completed_at: '2026-03-05T03:15:02.325858'
---

# Objective

Connect live job_listings to the matching pipeline (backend) and display them on the plan page (frontend). Jobs from DB populate plan.job_matches with credit/transit filtering.

# Files to Update

| File | Change |
|------|--------|
| `backend/app/modules/matching/engine.py` | MODIFY: generate_plan queries job_listings, builds JobMatch list |
| `backend/app/routes/jobs.py` | MODIFY: add source field to enriched output |
| `frontend/src/app/plan/page.tsx` | MODIFY: add "Explore More Jobs" section via getJobs() |
| `backend/tests/test_live_jobs_merge.py` | NEW: test merge logic |

# Implementation Plan

1. Write backend tests: plan includes jobs, credit filter, empty DB graceful
2. In generate_plan: query get_all_job_listings, convert to JobMatch, apply credit filter
3. Add _build_job_matches helper in engine.py
4. Add source field to enriched job output in jobs.py
5. Frontend: secondary useQuery for getJobs({barriers}), render "Explore More Jobs" section
6. Handle empty job_listings gracefully (empty list, no crash)

# Acceptance Criteria

- [x] generate_plan populates job_matches from job_listings table
- [x] Credit-requiring jobs marked eligible_now=False for credit-barrier users
- [x] Frontend displays "Explore More Jobs" section with live listings
- [x] Empty job_listings degrades gracefully
- [x] source field present in enriched job responses
- [x] 100% test coverage for merge logic

# Verification

```bash
cd backend && python -m pytest tests/test_live_jobs_merge.py tests/test_engine.py -xvs
python -m pytest tests/ --cov=app --cov-report=term-missing --cov-fail-under=100 -q
```