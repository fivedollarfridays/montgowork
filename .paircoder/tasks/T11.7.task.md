---
id: T11.7
title: Docker + DevOps — containerization, deps, gitignore, production
plan: plan-2026-03-launch-prep-polish
type: chore
priority: P1
complexity: 40
status: done
sprint: 11
tags:
- devops
- docker
depends_on: []
trello_card_id: '65'
completed_at: '2026-03-05T12:57:52.747303'
---

# Objective

Add Docker support for demo deployment, pin Python deps, fix gitignore, add production safeguards.

# Files to Create/Update

| File | Change |
|------|--------|
| Dockerfile | NEW: Multi-stage build (Node + Python) |
| docker-compose.yml | NEW: Single-service compose for demo |
| backend/requirements.txt | Pin all deps to exact installed versions |
| .gitignore | Add .coverage, tsconfig.tsbuildinfo, .paircoder/cache/ |
| backend/app/main.py | Disable Swagger docs when ENVIRONMENT=production |

# Implementation Plan

1. **Dockerfile**: Node stage builds frontend, Python stage serves backend + static
2. **docker-compose.yml**: Port 8000, env_file, SQLite volume
3. **requirements.txt**: `pip freeze` exact versions
4. **.gitignore**: Add missing entries
5. **main.py**: `docs_url=None, redoc_url=None` when production

# Acceptance Criteria

- [x] `docker compose build` succeeds
- [x] All Python deps pinned
- [x] .gitignore covers build artifacts
- [x] Swagger disabled in production mode

# Verification

```bash
docker compose build
git status  # no untracked build artifacts
```