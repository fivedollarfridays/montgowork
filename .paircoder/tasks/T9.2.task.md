---
id: T9.2
title: docs/api.md — endpoint reference with examples
plan: plan-2026-03-docs-and-readme
type: chore
priority: P1
complexity: 25
status: pending
sprint: 9
tags: [docs]
depends_on: []
trello_card_id: "51"
---

# Objective

Create docs/api.md with a complete endpoint reference — every route, method, request/response shapes, and curl examples.

# Files to Update

| File | Change |
|------|--------|
| docs/api.md | NEW — API endpoint reference |

# Implementation Plan

1. Read all route files: assessment.py, plan.py, credit.py, jobs.py, brightdata.py
2. Read all relevant type files for request/response shapes
3. Write docs/api.md with:
   - **Base URL**: http://localhost:8001
   - **Endpoints table**: method, path, description
   - **Per-endpoint sections**: request body (JSON), response body (JSON), status codes, curl example
   - Endpoints to document:
     - `GET /` — health check
     - `POST /api/assessment/` — create assessment session
     - `GET /api/plan/{session_id}` — get re-entry plan
     - `POST /api/plan/{session_id}/narrative` — generate AI narrative
     - `POST /api/credit/assess` — credit assessment proxy
     - `GET /api/jobs/` — list job listings
     - `POST /api/brightdata/crawl` — trigger BrightData crawl
     - `GET /api/brightdata/status/{snapshot_id}` — crawl status
     - `POST /api/brightdata/precrawl` — pre-crawl Montgomery jobs

# Acceptance Criteria

- [ ] docs/api.md exists with endpoint reference table
- [ ] Every backend route is documented with method + path
- [ ] Request/response JSON shapes documented for each endpoint
- [ ] At least one curl example per endpoint
- [ ] Status codes documented (200, 400, 404, 422, 503)
- [ ] All shapes match actual Pydantic models in the codebase

# Verification

```bash
# File exists
wc -l docs/api.md

# All routes covered
grep "POST\|GET" docs/api.md | wc -l
```
