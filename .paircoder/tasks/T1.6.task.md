---
id: T1.6
title: Test infrastructure (conftest + test stubs)
plan: plan-2026-03-module-skeletons
type: feature
priority: P1
complexity: 25
status: done
sprint: null
tags:
- tests
- pytest
- stubs
depends_on:
- T1.4
- T1.5
trello_card_id: '6'
completed_at: '2026-03-04T19:31:12.198573'
---

# Objective

Create test infrastructure under `backend/tests/` with a `conftest.py` providing fixtures (test engine, test client) and test stub files for filters and assessment. Test stubs use `NotImplementedError` -- Vinny writes the bodies alongside implementations.

# Files to Create

| File | Change |
|------|--------|
| `backend/tests/__init__.py` | Empty file for pytest discovery |
| `backend/tests/conftest.py` | `anyio_backend`, `test_engine`, `client` fixtures |
| `backend/tests/test_filters.py` | TestCreditFilter (3), TestTransitFilter (3), TestChildcareFilter (1), TestCertificationRenewal (5) -- 12 test stubs |
| `backend/tests/test_assessment.py` | TestAssessmentEndpoint (5) -- 5 test stubs |

# Implementation Plan

1. Create `backend/tests/__init__.py` (empty)
2. Create `conftest.py` with:
   - `anyio_backend` fixture (returns "asyncio")
   - `test_engine` async fixture (tmp_path SQLite, clears get_settings cache, resets _engine global)
   - `client` fixture (depends on test_engine, returns TestClient)
3. Create `test_filters.py` with 12 test stubs across 4 test classes
4. Create `test_assessment.py` with 5 test stubs in 1 test class
5. Verify pytest discovers all 17 tests

# Acceptance Criteria

- [x] `backend/tests/__init__.py` exists
- [x] `conftest.py` has `test_engine` fixture that uses tmp_path (not production DB)
- [x] `conftest.py` `test_engine` clears `get_settings` lru_cache and resets `_engine` global
- [x] `conftest.py` `client` fixture depends on `test_engine`
- [x] `test_filters.py` has 12 test methods across 4 classes
- [x] `test_assessment.py` has 5 test methods in TestAssessmentEndpoint
- [x] All test stubs raise `NotImplementedError`
- [x] `pytest --collect-only` discovers all 17 tests
- [x] No test file exceeds 300 lines

# Verification

```bash
cd backend
source .venv/bin/activate
pytest --collect-only tests/ 2>&1 | tail -5
bpsai-pair arch check backend/tests/conftest.py backend/tests/test_filters.py backend/tests/test_assessment.py
```