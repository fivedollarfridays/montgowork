---
id: T11.3
title: Test coverage — assessment errors, barrier types, network faults
plan: plan-2026-03-launch-prep-polish
type: chore
priority: P0
complexity: 35
status: pending
sprint: 11
tags: [testing, backend]
depends_on: [T11.1]
trello_card_id: "61"
---

# Objective

Close critical test coverage gaps: assessment route error paths, untested barrier types, BrightData network errors, and narrative double-fault.

# Files to Update

| File | Change |
|------|--------|
| backend/tests/test_assessment.py | Add error path tests (DB failure, engine failure, invalid payload) |
| backend/tests/test_engine.py | Add HOUSING, HEALTH, CRIMINAL_RECORD barrier type tests |
| backend/tests/test_brightdata_client.py | Add httpx.TimeoutException and ConnectError tests |
| backend/tests/test_plan_route.py | Add narrative double-fault test (Claude fails, verify actual fallback runs) |

# Implementation Plan

1. **test_assessment.py**: Mock create_session to raise, verify 500. Mock generate_plan to raise, verify error response
2. **test_engine.py**: Test barrier card generation for HOUSING (social_service), HEALTH (social_service), CRIMINAL_RECORD (career_center + social_service)
3. **test_brightdata_client.py**: Mock httpx to raise TimeoutException, verify propagation
4. **test_plan_route.py**: Mock generate_narrative to raise, do NOT mock build_fallback_narrative, verify fallback content is returned

# Acceptance Criteria

- [ ] Assessment route: DB failure returns 500
- [ ] Assessment route: Engine failure handled gracefully
- [ ] HOUSING, HEALTH, CRIMINAL_RECORD barriers produce correct cards
- [ ] BrightData client: timeout errors tested
- [ ] Narrative fallback integration tested without mocking fallback
- [ ] All tests pass

# Verification

```bash
cd backend && python -m pytest tests/ -v --tb=short
python -m pytest tests/ --cov=app --cov-report=term-missing -q
```
