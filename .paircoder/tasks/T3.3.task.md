---
id: T3.3
title: Test database lifecycle + seed edge cases
plan: plan-2026-03-test-coverage
type: chore
priority: P0
complexity: 25
status: done
sprint: 3
tags:
- testing
depends_on: []
trello_card_id: '13'
completed_at: '2026-03-04T20:26:56.912896'
---

# Objective

Cover `app/core/database.py` (74% → 100%). Test lifecycle functions and seed edge cases.

# Files to Update

| File | Change |
|------|--------|
| `backend/tests/test_database.py` | Add tests for lifecycle functions and seed edge cases |

# Implementation Plan

1. Test `get_engine()`: first call creates engine, second returns cached global
2. Test `get_async_session_factory()`: creates factory from engine, caches it
3. Test `get_db()`: yields async session, properly closes
4. Test `close_db()`: disposes engine, resets globals to None
5. Test `seed_database()` skip path: when resources already exist (count > 0), returns early
6. Test `seed_database()` missing file: when JSON file doesn't exist, continues to next
7. Test `seed_database()` empty record: when `_validate_seed_record` returns empty dict, skips insert

# Acceptance Criteria

- [x] `database.py` at 100% coverage
- [x] Tests cover `get_engine`, `get_async_session_factory`, `get_db`, `close_db`
- [x] Tests cover seed skip (already seeded), missing file, empty record edge cases
- [x] Uses tmp_path SQLite — no production DB
- [x] All tests pass

# Verification

```bash
pytest tests/test_database.py -v
pytest tests/ --cov=app/core/database --cov-fail-under=100
```